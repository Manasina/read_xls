import warnings
warnings.simplefilter("ignore")

# data = [dict(data) for data in records]
# result = pd.DataFrame(data)
# dataKeys = list(result.keys())
# cursor.execute(f"""
#         INSERT INTO Customers ({','.join(list(result.keys()))})
# VALUES ({});""")
# print(cursor)
